apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: weather
      defaultBucket: weather
    secureJsonData:
      token: weather-station-token
    isDefault: true
